    /* preisFruechteTotal = preisFruechte.reduce((total, price) => total + price, 0);
    preisSuessTotal = preisSuess.reduce((total, price) => total + price, 0);
    preisNuesseTotal = preisNuesse.reduce((total, price) => total + price, 0);
    preisSuperfoodsTotal = preisSuperfoods.reduce((total, price) => total + price, 0); */